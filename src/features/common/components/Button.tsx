import { styled } from '../../../stitches.config';

export const Button = styled('button', {
  backgroundColor: '$indigo12',
  color: 'white',
  padding: '8px 16px',
  borderRadius: '6px',
  fontWeight: 700,
});
